import { fsdActivation20AxiosService } from './axios-service';

describe('fsdActivation20AxiosService', () => {
  it('should work', () => {
    expect(fsdActivation20AxiosService()).toEqual(
      'fsd-activation-2.0-axios-service'
    );
  });
});
